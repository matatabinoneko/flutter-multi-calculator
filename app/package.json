{
    "scripts": {
        "create:freezed": "flutter pub run build_runner build --delete-conflicting-outputs",
        "flutter:run": ". ./ios/Flutter/flutter_export_environment.sh; flutter run"
    }
}